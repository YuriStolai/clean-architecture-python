[project]
name = "clean-architecture-python"
version = "0.1.0"
description = ""
authors = [
    {name = "Yuri Stolai"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
]

[tool.poetry]
packages = [{include = "clean_architecture_python", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
